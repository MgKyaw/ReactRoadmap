// const multiply = (multiplier) => (multiplicand) =>
//   multiplicand * multiplier;

// const product = multiply(3)(4);

// console.log(product);
// // 12

const multiply = (multiplier, multiplicand) =>
  multiplicand * multiplier;

const product = multiply(3, 4);

console.log(product);
// 12